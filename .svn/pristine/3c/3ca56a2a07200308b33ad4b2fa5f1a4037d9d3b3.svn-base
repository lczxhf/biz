require 'rails_helper'

RSpec.describe Wap::LotteryOrderController, type: :controller do
	before(:each) do 
		assigns[:current_user] = User.first
	end
	describe 'get #pay' do
		it 'test' do
			get 'index'
			 expect(response).to have_http_status(:success)
	end
	end

end
